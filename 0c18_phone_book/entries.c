/**
* Program: book.c
* Purpose: in a program to maintain a list of phone numbers,
    A structure is defined to hold each entry of a person’s name and number
*/
#include "main.h"

int main(void)
{
        printf("Name: %s %s\n",y2023[0].buyerDetails.person.lastName, y2023[0].buyerDetails.person.firstName);
        printf("Firm: %s\n",y2023[0].buyerDetails.firm);
        printf("Contact: %s\n",y2023[0].buyerDetails.person.phone);

    return 0;
}